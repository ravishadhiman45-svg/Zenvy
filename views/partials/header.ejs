<header>
  <div class="flex items-center justify-between p-4">
    <h1 class="text-2xl font-bold">Zenvy</h1>
    <% var islogin = (typeof islogin !== 'undefined') ? islogin : true; %>
    <% if (islogin) { %>
      <div class="flex gap-4 text-lg flex-wrap">
        <a href="/shop">Shop</a>
        <a href="/cart">Cart</a>
        <a href="/account">My Account</a>
        <a href="/users/logout" class="text-red-500 hover:underline">Logout</a>
      </div>
    <% } %>
  </div>
</header>
